import { RoomTag } from "../rooms/types";
import { StudentTag } from "../students/types";
import { DormitoryTag } from "../dormitories";
import { RoleTag } from "../roles";
import { UserTag } from "../users";

export const Tags = [StudentTag, RoomTag, DormitoryTag, RoleTag, UserTag];
